<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Gym member, attendance, payment, and renewal records are intentionally excluded
         from automatic Android backup. Use GymPro's explicit export/import flow instead. -->
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
    <exclude domain="file" path="." />
</full-backup-content>
