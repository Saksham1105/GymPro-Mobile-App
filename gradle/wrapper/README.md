The Gradle Wrapper JAR is generated by Gradle and is intentionally not recreated manually here. Before relying on wrapper-based builds, generate and commit `gradle-wrapper.jar` together with `gradlew` and `gradlew.bat` using a local Gradle installation.
